# MinIO client for one-shot provisioning of the lakehouse object store:
# creates the warehouse bucket and the non-root user Lakekeeper uses for
# STS credential vending (MinIO refuses AssumeRole for root credentials).
FROM minio/mc:RELEASE.2025-08-13T08-35-41Z
